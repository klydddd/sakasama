/// Centralized UI dimension constants for the Sakasama app.
///
/// All spacing, sizing, and typography values live here to keep
/// the design system consistent and avoid magic numbers in widgets.
class AppDimensions {
  AppDimensions._();

  // ── Button Heights ──────────────────────────────────────────────────────
  static const double primaryButtonHeight = 72.0;
  static const double secondaryButtonHeight = 56.0;
  static const double listItemHeight = 72.0;
  static const double bottomNavHeight = 72.0;

  // ── Font Sizes ──────────────────────────────────────────────────────────
  static const double displaySize = 28.0;
  static const double headingSize = 24.0;
  static const double titleSize = 20.0;
  static const double bodySize = 18.0;
  static const double captionSize = 16.0;
  static const double buttonTextSize = 20.0;
  static const double smallTextSize = 14.0;

  // ── Spacing ─────────────────────────────────────────────────────────────
  static const double screenPadding = 20.0;
  static const double cardPadding = 16.0;
  static const double itemSpacing = 16.0;
  static const double sectionSpacing = 32.0;
  static const double smallSpacing = 8.0;
  static const double tinySpacing = 4.0;

  // ── Border Radius ───────────────────────────────────────────────────────
  static const double cardRadius = 16.0;
  static const double buttonRadius = 14.0;
  static const double chipRadius = 24.0;
  static const double sheetRadius = 24.0;
  static const double inputRadius = 12.0;

  // ── Icons ───────────────────────────────────────────────────────────────
  static const double iconSizeLarge = 48.0;
  static const double iconSizeMedium = 32.0;
  static const double iconSizeSmall = 24.0;

  // ── Action Button Card ──────────────────────────────────────────────────
  static const double actionCardMinHeight = 120.0;
  static const double actionCardIconSize = 40.0;

  // ── Progress Card ───────────────────────────────────────────────────────
  static const double progressRingSize = 80.0;
  static const double progressRingStroke = 8.0;

  // ── Microphone Button ───────────────────────────────────────────────────
  static const double micButtonSize = 80.0;
}
